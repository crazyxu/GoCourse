<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>
    
    <!--Theme.MCourse for MCourse-->
    <style name="Theme.MainActivity" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/Theme.MainActivity.ActionBarStyle</item>
    </style>

    <!--actionBarStyle for Theme.MCourse-->
    <style name="Theme.MainActivity.ActionBarStyle" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@drawable/abc_cab_background_internal_bg</item>
    </style>

</resources>
